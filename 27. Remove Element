public class Solution {
//keep two pointers. one for iterate, one as the new array
    public int removeElement(int[] nums, int val) {
        
    int begin=0;
    for(int i=0;i<nums.length;i++) 
    if(nums[i]!=val) 
    nums[begin++]=nums[i];//*********
    return begin;
    }
}